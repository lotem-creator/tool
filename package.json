{
  "name": "lotem-ad-tool",
  "version": "1.0.0",
  "description": "Lotem's PPC Ad Tool - Netlify deployment",
  "dependencies": {
    "node-fetch": "^2.7.0",
    "cheerio": "^1.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site - no build needed'"
  }
}
